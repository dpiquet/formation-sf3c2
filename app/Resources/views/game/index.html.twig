{% extends 'base.html.twig' %}

{% block content %}
    <h2>{{ 'subtitle.game_index'|trans }}</h2>

    <p class="attempts">
        {{ 'game.remaining_attempts'|transchoice(11) }}
    </p>

    <ul class="word_letters">
        {% for l in 1..8 %}
            <li class="letter hidden">
                ?
            </li>
        {% endfor %}
    </ul>

    <br class="clearfix" />

    <p class="attempts">
        <a href="/game/reset">{{ 'game.reset'|trans }}</a>
    </p>

    <br class="clearfix" />

    <h2>{{ 'game.try_letter'|trans }}</h2>

    <ul>
        {% for l in 'A'..'Z' %}
            <li class="letter">
                <a href="/game/letter/{{ l }}">
                    {{ l }}
                </a>
            </li>
        {% endfor %}
    </ul>

    <h2>{{ 'game.try_word'|trans }}</h2>

    <form action="/game/word" method="post">
        <p>
            <label for="word">{{ 'game.word'|trans }}</label>
            <input type="text" id="word" name="word"/>
            <button type="submit">{{ 'game.guess'|trans }}</button>
        </p>
    </form>
{% endblock %}
